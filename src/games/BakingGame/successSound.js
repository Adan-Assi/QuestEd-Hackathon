// This is a simple JavaScript file to handle importing the sound file
// It's needed because React can't directly import MP3 files in JSX files

const successSound = '/assets/success-sound.mp3';

export default successSound;
